<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hello.petshop.mapper.TestUserMapper">
   <!--<select id="loadUser" resultType="TestUser" parameterType="TestUser">-->
        <!--select * from usertable where UserName=#{userName} and UserPwd=md5(#{userPwd})-->
    <!--</select>-->

    <select id="loadUser" resultType="testuser">
        select * from usertable where UserId = 1;
    </select>
</mapper>
